<% include ../partials/header %>
<% include ../partials/menu %>
	<div class="container">
		<br>
		<div class="row">
			<div class="col-4">
				<div class="card">
					<%
           if(player_info){
             var image_src = player_info[0].img_src;
           }else{
             var image_src = '../resources/img/helmet.jpg';
           }
 				 %>

<!-- TODO: Modify the ejs code below, to conditionally set the image_src to be from our chosen player -->


				 <img class="card-img-top" id="player_img" src= "<%- image_src %>" alt="Image of Football Player">
				  <div class="card-body mx-auto">
					<form class="form"action="/player_info/select_player" method="get">
						<select class="custom-select" name="player_choice" onchange="this.form.submit()">
							 <option selected>Select Player</option>

							 <%  if(players_data) {
                    var players = '';
                    players_data.forEach(player => {
                          players += '<option value="' + player.id + '">' + player.name + '</option>\n';
                        })                                                                     }                                                                        %>

            <%- players %>

             <!-- TODO: Populate our select with player options
							 			For each player, set a new option with a value matching the palyer's id,
							 			but will display the player's name-->
						</select>
					</form>
				  </div>
				</div>
			</div>
			<div class="col-8">
				<div class="table-responsive">
					<table class="table">
						<tr>
							<%
	            if(player_info && games_played) {
	              games_played = games_played[0].count;
	              player_info = player_info[0];

	              console.log(games_played, player_info);

	              var year = '' + player_info.year;
	              var major = '' + player_info.major;
	              var games_played = '' + games_played;

	              var passing_yards = '' + player_info.passing_yards;
	              var avg_passing_yards = '' + player_info.passing_yards / games_played;

	              var rushing_yards = '' + player_info.rushing_yards;
	              var avg_rushing_yards = '' + player_info.rushing_yards / games_played;

	              var receiving_yards = '' + player_info.receiving_yards;
	              var avg_receiving_yards = '' + player_info.receiving_yards / games_played;

								var neato = "Wow that's neat";
	            }
	            %>
							<!-- TODO: Set Player Information
							 			If a player has been selected, fill in their information in each of the fields below.
							 		    You'll need to calculate here the player's average rushing, receiving, and passing yards.-->
							<th>Year</th>
							<td> <%- year %>	</td>
						</tr>
						<tr>
							<th>Major</th>
							<td> <%- major %>	</td>
						</tr>
						<tr>
							<th>Games Played</th>
							<td> <%- games_played %>		</td>
						</tr>
						<tr>
							<td>Passing Yards: <%- passing_yards %>	</td>
							<td>Avg. Passing Yards: <%- avg_passing_yards  %>	</td>
						</tr>
						<tr>
							<td>Rushing Yards: <%- rushing_yards %> </td>
							<td>Avg. Rushing Yards:  <%- avg_passing_yards  %> </td>
						</tr>
						<tr>
							<td>Receiving Yards:  <%- receiving_yards  %></td>
							<td>Avg. Receiving Yards:  <%- avg_passing_yards  %> </td>
						</tr>
					</table>
				</div>
			</div>
		</div>
	</div>

<% include ../partials/footer %>
